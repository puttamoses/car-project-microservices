apiVersion: v1
kind: Service
metadata:
  name: car-frontend
  namespace: autoelite

spec:
  selector:
    app: car-frontend

  type: LoadBalancer

  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
