# k8s/frontend-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: car-frontend
  namespace: autoelite
spec:
  selector:
    app: car-frontend
  type: ClusterIP          # ← was LoadBalancer; Ingress handles external traffic
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
